auto numbers = [10, 20, 30, 40, 50];
I64 sum = 0;

foreach (n; numbers) {
    sum = sum + n;
}

Print("Array elements:\n");

foreach (n; numbers) {
    Print("  ");
    Print(n);
    Print("\n");
}

Print("Total sum: ");
Print(sum);
Print("\n");
