/* src/impl/kernel/memory/memory.c — placeholder TU.
 *
 * Intentionally empty. memory.h is the umbrella header reserved for
 * future cross-module memory helpers; today every memory caller pulls in
 * pmm/vmm/heap directly. Kept around so the build graph has a stable
 * .c file to compile under this name.
 */
#include "memory/memory.h"
