/* src/intf/memory/memory.h — placeholder umbrella header.
 *
 * Intentionally empty. Reserved for future cross-module memory helpers
 * that don't fit in pmm/vmm/heap. Today every memory caller includes
 * the specific subsystem header directly.
 */
#ifndef MEMORY_H
#define MEMORY_H



#endif
